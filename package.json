{
  "name": "launchpad-lab",
  "private": true,
  "workspaces": [
    "apps/frontend",
    "apps/worker",
    "packages/shared"
  ],
  "scripts": {
    "build": "npm run -w @launchpad/shared build && npm run -w @launchpad/frontend build && npm run -w @launchpad/worker build",
    "dev": "npm run -w @launchpad/frontend dev",
    "typecheck": "npm run -w @launchpad/shared build && npm run -w @launchpad/frontend typecheck && npm run -w @launchpad/worker typecheck"
  }
}
